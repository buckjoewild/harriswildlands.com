{
  "name": "BruceOps Family Steward",
  "description": "MCP configuration for Claude Desktop integration with BruceOps Family Steward",
  "version": "1.0.0",
  "instructions": {
    "setup": [
      "1. Open Claude Desktop settings",
      "2. Navigate to MCP (Model Context Protocol) configuration",
      "3. Add this server configuration",
      "4. Restart Claude Desktop"
    ],
    "usage": [
      "Ask Claude to analyze your family data export",
      "Use natural language: 'Analyze my family's week for drifts'",
      "Claude will read the exported JSON and provide insights"
    ]
  },
  "mcpServers": {
    "bruceops-family": {
      "command": "node",
      "args": ["family-steward/mcp-server.js"],
      "env": {
        "BRUCEOPS_API_URL": "http://localhost:5000",
        "BRUCEOPS_DATA_DIR": "./family-steward"
      }
    }
  },
  "tools": [
    {
      "name": "read_family_export",
      "description": "Read the latest family data export from BruceOps",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "name": "analyze_family_drifts",
      "description": "Analyze family data for patterns and drifts that need attention",
      "inputSchema": {
        "type": "object",
        "properties": {
          "focus": {
            "type": "string",
            "description": "Optional focus area: energy, connection, goals, or all",
            "enum": ["energy", "connection", "goals", "all"]
          }
        },
        "required": []
      }
    },
    {
      "name": "suggest_family_activities",
      "description": "Generate bond-strengthening activity suggestions based on current family patterns",
      "inputSchema": {
        "type": "object",
        "properties": {
          "effort_level": {
            "type": "string",
            "description": "Maximum effort level for suggestions",
            "enum": ["low", "medium", "high"]
          },
          "time_available": {
            "type": "string",
            "description": "Available time for activities",
            "enum": ["15min", "30min", "1hour", "halfday", "fullday"]
          }
        },
        "required": []
      }
    },
    {
      "name": "write_import_suggestions",
      "description": "Write AI-generated suggestions to a file that BruceOps can import",
      "inputSchema": {
        "type": "object",
        "properties": {
          "suggestions": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "activity": { "type": "string" },
                "rationale": { "type": "string" },
                "effort": { "type": "string" }
              }
            }
          }
        },
        "required": ["suggestions"]
      }
    }
  ],
  "resources": [
    {
      "uri": "file://family-steward/input.json",
      "name": "Family Export Data",
      "description": "Latest exported family data from BruceOps",
      "mimeType": "application/json"
    },
    {
      "uri": "file://family-steward/output.json",
      "name": "Analysis Results",
      "description": "Results from family drift analysis",
      "mimeType": "application/json"
    }
  ],
  "prompts": [
    {
      "name": "analyze_week",
      "description": "Analyze the family's week for drifts and patterns",
      "arguments": []
    },
    {
      "name": "suggest_activities",
      "description": "Suggest bond-strengthening activities",
      "arguments": [
        {
          "name": "context",
          "description": "Optional context about current family situation",
          "required": false
        }
      ]
    }
  ]
}
